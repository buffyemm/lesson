#include <iostream>


int main()
{
    int a = 5;// переменная a, типа int, целые переменные
    float b = 4.9;// переменная b, типа float, переменные с плавающей запятой
    char x = 'A'; // переменная х, типа char, которая содержит в себе только символы

    std::cout << "Hello World!\n";//вывод в консоль нашего текста
    std::cout << a;// вывод в консоль, что содержиться в переменной a
}

